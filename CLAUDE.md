# 项目配置

## 部署偏好

- **自动推送代码**：每次修改完成后，自动执行 `git add -A`、`git commit`、`git push gitee` 和 `git push github` 推送到远程仓库
- **远程仓库**：
  - Gitee: https://gitee.com/bacashi/bacashi.git
  - GitHub: https://github.com/Siocny/bacashi.git

## 项目结构

- 静态 HTML/CSS/JavaScript 网站
- 使用 localStorage 模拟后端数据存储
- 管理后台位于 `admin/index.html`
